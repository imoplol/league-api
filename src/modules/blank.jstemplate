/**
 * blank
 *
 * @author  Siyuan Gao <siyuangao@gmail.com>
 * @license MIT
 */

//  This is a sample if what the template should look like

import  Promise     from 'bluebird';

function module (options) {
    return new Promise((resolve, reject) => {   //  Must use arrow function for `this` to be passed correctly
        if("life is good"){
            resolve({something: 'anything'});
        } else {
            reject({anything: 'nothing'});
        }
    });
}

export default module;